apiVersion: v1
kind: Service
metadata:
  name: stock-predictor-service
spec:
  type: LoadBalancer
  selector:
    app: stock-predictor
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000